# pg_hba.conf — ACCEPT FROM ANYWHERE WITH PASSWORD

# Local Unix socket + loopback
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# Anywhere IPv4/IPv6 with password auth
host    all             all             0.0.0.0/0               scram-sha-256
host    all             all             ::/0                    scram-sha-256
# (If clients still use MD5 hashed passwords, swap scram-sha-256 for md5.)
