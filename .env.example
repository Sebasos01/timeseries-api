# Gateway
NODE_ENV=development
HTTPS_PORT=8443
HTTP_PORT=8080
PUBLIC_HTTPS_PORT=8443
# HTTP_MODE options: serve (plain HTTP + HTTPS), redirect (force HTTPS), off (disable HTTP listener)
HTTP_MODE=serve
# Override if your certs live elsewhere
SSL_KEY_PATH=../certs/server.key
SSL_CERT_PATH=../certs/server.crt
JAVA_API_URL=http://service:8080

# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=timeseries
POSTGRES_HOST_PORT=5432
POSTGRES_PORT=5432

# Security
AUTH_ENABLED=false
OAUTH_JWKS_URI=http://localhost:8082/.well-known/jwks.json
OAUTH_AUDIENCE=timeseries-api
OAUTH_ISSUER=https://example.auth0.com/
ADMIN_SCOPE=admin:reindex
# Optional: set ADMIN_ROLE + ADMIN_ROLE_CLAIM to enforce a role-based claim instead of scope
# ADMIN_ROLE=admin
# ADMIN_ROLE_CLAIM=roles

# Open Search
OPENSEARCH_URL=http://search:9200
OPENSEARCH_INITIAL_ADMIN_PASSWORD=glphoBriJUkEs=#rawR-y7va4ruJus
